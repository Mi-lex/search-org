import React from 'react';
import classes from './SavedOrgsCounter.module.css';

const SavedOrgsCounter = () => {
    return <span className={classes.counter}> (0)</span>;
};

export default SavedOrgsCounter;
